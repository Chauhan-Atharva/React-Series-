import React from 'react';//react is core foundational library
import ReactDOM from 'react-dom/client';//implementation of react for web

import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));//create a dom of react
root.render(//creates a dom and renders it 
  
    <App /> 
  
);


